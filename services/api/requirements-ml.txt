# Heavy ML/audio backends. Install on top of requirements.txt when you want real output.
# CPU-only torch: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
demucs>=4.0
torch>=2.4
torchaudio>=2.4
basic-pitch>=0.4
librosa>=0.10
numpy>=1.26
matchering>=2.0
soundfile>=0.12
