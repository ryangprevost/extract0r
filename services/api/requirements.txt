fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12
# Ingest probing and canonical decoding. Small wheels, no system dependencies.
numpy>=1.26
soundfile>=0.12
soxr>=0.5
# m4a/aac ingest. libsndfile has never read AAC, so before this the upload form
# advertised m4a and then failed at decode unless a system ffmpeg happened to be on
# PATH. PyAV carries the ffmpeg libraries inside the wheel, so it needs no install.
av>=13
# Phase 2 mastering and export, none of which needs ffmpeg:
#   scipy      - spectral maths
#   lameenc    - MP3 encoding, a LAME binding rather than a subprocess
#   pyloudnorm - EBU R128 / ITU-R BS.1770 loudness, so LUFS means LUFS
scipy>=1.11
lameenc>=1.7
pyloudnorm>=0.1
# Fetching a reference from a URL. Already present transitively via FastAPI's test
# client; pinned here because a route depends on it at runtime.
httpx>=0.27
